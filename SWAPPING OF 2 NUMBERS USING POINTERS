#include<stdio.h>
void swap(int *a,int *b)
{
    int temp;
    temp=*a;
    *a=*b;
    *b=temp;
}
void main()
{
    int a,b;
    printf("enter the number for x,y:");
    scanf("%d%d",&a,&b);
    swap(&a,&b);
    printf("the values after swapping:%d,%d",a,b);
}
